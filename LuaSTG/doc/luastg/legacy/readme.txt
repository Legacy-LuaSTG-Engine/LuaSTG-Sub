Note:

The modules in this folder are used for code completion
and are divided into multiple files for maintenance purposes only.
These modules cannot be accessed through require("legacy.XXX")!

注意：

该文件夹内的模块用于代码提示，分多个文件只是为了方便维护，
无法通过 require("legacy.XXX") 来访问这些模块！
